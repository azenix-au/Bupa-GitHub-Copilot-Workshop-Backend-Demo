# How to Run the CoffeeOrder Project

```
dotnet run
```

Once the application is running, you can access the API at `http://localhost:5000`. Use tools like Postman or `curl` to interact with the endpoints.